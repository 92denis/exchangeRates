import { FiterCurrensiesPipe } from './fiter-currensies.pipe';

describe('FiterCurrensiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiterCurrensiesPipe();
    expect(pipe).toBeTruthy();
  });
});
